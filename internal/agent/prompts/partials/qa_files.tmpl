{{- /*
  qa_files: lists the Q&A artifact paths captured during earlier phases so
  the agent reads prior operator responses instead of re-asking. An
  operator may be the end user, a delegated primary agent, or an automatic
  selector; the artifact carries any available provenance. Wording differs
  slightly between phases (whether to mention
  "do not re-ask"); the Lead field controls the lead sentence and the
  optional "do-not-re-ask" clause.
*/ -}}
{{- define "qa_files" -}}
{{ if .Paths }}## Prior Operator Responses

These responses came through the operator control surface. Do not call them end-user decisions or end-user approval unless the artifact explicitly identifies that provenance. Keep the section name "Prior Operator Responses" when referring to them. Never relabel them as "User Decisions", "Human Decisions", user agreement, or user approval. {{ .Lead }}
{{ range .Paths }}- {{ . }}
{{ end }}{{ if .TrailingBlank }}
{{ end }}{{ end }}{{- end -}}
